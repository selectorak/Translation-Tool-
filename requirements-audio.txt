# ====== 音频翻译依赖（录音 + 本地语音识别） ======
# 安装：pip install -r requirements-audio.txt

faster-whisper>=1.0.0
pycaw>=0.0.1
comtypes>=1.1.14
numpy>=1.24.0

# ====== GPU 加速（可选，RTX 显卡推荐） ======
# nvidia-cublas-cu12  # 安装后需手动复制 DLL
